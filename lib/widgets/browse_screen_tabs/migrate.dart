import 'package:flutter/material.dart';

class Migrate extends StatelessWidget {
  const Migrate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
