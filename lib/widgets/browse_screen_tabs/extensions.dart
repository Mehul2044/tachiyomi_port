import 'package:flutter/material.dart';

class Extensions extends StatelessWidget {
  const Extensions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
